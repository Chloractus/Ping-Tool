# Ping-Tool
  Basic Python Ping Script using Scapy.

  This Python script utilizes basic packet creation to ping a designated target. It sends a specific number of ping packets to the target, waits for a response, then returns the statistics in the terminal. The statistics are formated similarly to the Ping command as this script is meant to mimic how Ping works.
